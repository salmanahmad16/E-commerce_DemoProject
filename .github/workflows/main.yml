name: Cypress test3
on: [push]
jobs:
  cypress-run:
    runs-on: macos-10.15
    steps:
    - name: Cypress.io
      uses: cypress-io/github-action@v4.2.0
      with:
        start: npm run startTest
